'use strict';

import React from 'react';
import { connect } from 'react-redux';

export const Loading = () => {

  return (
    <aside className={'loading'}><div className={'loading__icon'} /></aside>
  );

};

export default Loading;
